@model IEnumerable<OnlineLibrary.Models.Book>

<div class="card-deck">
    @foreach (var book in Model)
    {
        <div class="card text-black col-lg-4" style="background-color:white;">
            @Html.Partial("_BookPartial", book)
        </div>
    }
</div>