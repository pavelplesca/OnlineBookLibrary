@model IEnumerable<OnlineBookLibrary.Models.Book>

<div class="card-deck ">
    @foreach (var book in Model)
    {
        <div class="card text-white" style="background-color:black;">
            @Html.Partial("_BookPartial", book)
        </div>
    }
</div>